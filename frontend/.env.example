# Backend API (server-side only — used by Next.js API proxy routes)
BACKEND_URL=http://localhost:8002
INTERNAL_API_KEY=

# Privy (public — required by SDK)
NEXT_PUBLIC_PRIVY_APP_ID=

# Vercel will also need NEXT_PUBLIC_API_URL for any direct backend calls
NEXT_PUBLIC_API_URL=http://localhost:8002
